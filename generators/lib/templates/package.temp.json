{
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "format": "prettier --write \"./**/*.{js,ts,json,yaml}\"",
    "test": "npm run format && mocha --require ts-node/register test/test-*.ts",
    "compile": "rm -rf dist && tsc",
    "prepublish": "npm run test && npm run compile"
  },
  "devDependencies": {
    "@types/chai": "^4.2.4",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.0.12",
    "chai": "^4.2.0",
    "mocha": "^6.1.4",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2"
  }
}
